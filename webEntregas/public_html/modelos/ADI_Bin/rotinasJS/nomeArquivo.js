<!-- /ADI_Intranet_Root/ADI_Programacao/ADI_Bin/rotinasJS/nomeArquivo.js -->/** * Função para retornar apenas o nome de um arquivo * @version 1.0 01/01/2000 * @author Alexandre * @param str Caminho (path) completo do arquivo * @return Nome do arquivo */function nomeArquivo(str){// início da função nomeArquivo()   posicao = 0;   for (x=str.length; x>0; x--)   {     barra = str.indexOf("/");     if (str.charAt(x)=="/")     {        posicao = x;        break;     }     if (str.charAt(x)=="\\")     {        posicao = x;        break;     }   }   // Antes a posicao era posicao+1, mas estava cortando o primeiro caracter no windows. No linux estava funcionando.   retorno = str.substring(posicao,str.length);   return retorno;}// fim da função nomeArquivo()