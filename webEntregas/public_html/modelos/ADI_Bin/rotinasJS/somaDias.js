<!-- /ADI_Intranet_Root/ADI_Programacao/ADI_Bin/rotinasJS/somaDias.js -->/** * Função que faz o somatório de dias em uma data  * @version 1.0 01/01/2000 * @author Alexandre * @param data Data de entrada * @param numDias Número de dias a serem somados * @param formatoEntrada Formato de entrada da data * @param formatoSaida Formato de saída da data * @return Data no formato MM/DD/YYYY com os dias somados*/ function somaDias(data, numDias, formatoEntrada, formatoSaida){    // inicio da function somaDias(data, numDias, formatoEntrada, formatoSaida)        diaWork = 0;    mesWork = 0;    anoWork = 0;     if (formatoEntrada.toUpperCase()=="MM/DD/YYYY"){        mesWork = data.substring(0,2);        diaWork = data.substring(3,5);        anoWork = data.substring(6,10);    }    else        if (formatoSaida.toUpperCase()=="DD/MM/YYYY") {            diaWork = data.substring(0,2);            mesWork = data.substring(3,5);            anoWork = data.substring(6,10);        }    mesWork--;    var objetoData = new Date(anoWork, mesWork, diaWork);    somaDia = numDias * 86400000;  /* A multiplicação é para atender à função getTime() */    total = objetoData.getTime() + somaDia;    objetoData.setTime(total);    diaWork = objetoData.getDate();    mesWork = objetoData.getMonth();    anoWork = objetoData.getFullYear();    mesWork++;    diaSaida = diaWork + "";    if (diaSaida.length==1) {        diaSaida = "0"+diaSaida;    }			    if (mesWork==0) {        mesWork = 12;    }    mesSaida = mesWork + "";    if (mesSaida.length==1) {        mesSaida = "0"+mesSaida;    }        if (formatoSaida.toUpperCase()=="DD/MM/YYYY") {        return diaSaida + "/" + mesSaida + "/" + anoWork;            }     else         if (formatoSaida.toUpperCase()=="MM/DD/YYYY") {            return mesSaida + "/" + diaSaida + "/" + anoWork;        }}// fim da function somaDias(data, numDias, formatoEntrada, formatoSaida)